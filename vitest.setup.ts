// You can add global test setup here if needed in the future.
// For example, import matchers: import '@testing-library/jest-dom/vitest';
// Note: If you add matchers like above, install the corresponding package:
// npm install --save-dev @testing-library/jest-dom